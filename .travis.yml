
language: python

python:
  - "3.3"
  - "3.4"

# command to install dependencies
install:
  - pip install lxml
  - pip install semantic_version
  - pip install coverage
  - pip install scrutinizer-ocular

# command to run tests
script:
  - "{ coverage run alldoctest.py && ocular ; } || false"
